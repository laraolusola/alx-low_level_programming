#include <stdio.h>

/**
 * main - prints all different combinations of two digits
 *
 *Return: The return value 0 (success)
 */
int main(void)
{
	int d, p;

	for (d = '0'; d < '9'; d++)
	{
	for (p = d + '1'; p <= '9'; p++)
	{
	if (p != d)
	{
	putchar(d);
	putchar(p);
	if (d < 8)
	putchar(',');
	putchar(' ');
	{
	{
	{
	putchar('\n);
	return (0);
}
